/*global describe, it, chai, sinon */
define([
	'salt',
	'dust',
	'backbone',
	'modules/Sort/SortRouter'
], function(SALT, dust, Backbone, Router) {

	var assert = chai.assert;

	var spec = {};

	describe('Sort Page Backbone Router', function() {
		/*
		it('should respond to the correct urls', function () {
			spec.router = new Router();
			Backbone.history.start();
			spec.spy = sinon.spy();
			spec.router.navigate('default', { trigger: true });
			spec.router.bind('route:handleContentType', spec.spy);
			spec.router.navigate('/content/articles/0', {trigger: true});
			spec.router.navigate('should-not-increase-callcount', {trigger: true});
			assert.ok(spec.spy.calledOnce);
			Backbone.history.stop();
		});
	*/
	});

});
